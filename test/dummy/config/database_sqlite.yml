default: &default
  adapter: sqlite3
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  database: db/active_record_embedded_test.sqlite3

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
