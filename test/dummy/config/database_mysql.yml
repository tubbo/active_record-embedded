default: &default
  adapter: mysql2
  # encoding: unicode
  # pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  database: active_record_embedded_test
  username: root

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
